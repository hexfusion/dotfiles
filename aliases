alias vi='vim'
alias ll='ls -l --color=always'
alias sshr='ssh -l root'
alias scpresume='rsync --partial --progress -ae ssh'
alias hosts='sudo vi /etc/hosts'
alias gf='git flow'
alias realias='$EDITOR ~/.aliases; source ~/.aliases'
alias git-archive-here='git archive --format=tar HEAD | gzip'
alias pod=perldoc

# these are from Yuval Kogman
alias ts="prove --state=slow,save -j3 -r"
alias tf="prove --state=failed,save -v -r"

alias ps?='ps -ef | grep'
alias s='sudo'

# git
alias g='git'
alias gs='git stash'
alias grh='git reset HEAD'
alias gpr='git pull --rebase'
alias reset='git reset HEAD~1 --soft'

# tmux
alias take='tmux detach -a'
alias give='tmux detach'
alias spin='tmux source-file ~/.tmux.conf'
alias stomp='tmux kill-session'
alias weather='weather KBGM'

# perl modules
alias release='RELEASE_TESTING=1 make test'

# punch
alias pi='punch in'
alias pl='punch list'
alias po='punch out'
alias apsum='archive-punch summary'
alias psum='punch summary'
alias ptot='punch total'

# camps
alias re='re --all'

alias fixssh='export $(tmux show-environment | grep \^SSH_AUTH_SOCK=)'

# tidy
alias retidy='_(){ tidy -config ~/.tidyconfig $1; }; _'
